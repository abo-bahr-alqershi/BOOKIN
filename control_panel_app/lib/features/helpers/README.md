Helpers feature: shared search/filter facades for Admin (Users, Properties, Units).

This module exposes simple facades that orchestrate existing repositories/usecases
to provide a consistent API for search and filtering without UI/widgets.

Structure:
- data/
- domain/
- presentation/ (intentionally empty: no UI here)

